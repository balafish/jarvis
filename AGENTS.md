# AGENTS.md — jarvis 寫作 / 簡報協作規則

本檔為既有 `CLAUDE.md` 的 Hermes/GPT 轉接版，保留 jarvis repo 最需要自動載入的工作法。

## Repo 定位
- 本 repo 以策略文件、品牌/公司內容撰寫、簡報與摘要圖產出為主。
- 完整手冊仍在 `CLAUDE.md`；本檔保留高頻規則與工具入口。

## 核心協作模式
- 預設流程：`討論 → 確認 → 產出 → 審核 → 推送`
- 討論階段：
  - 簡潔回覆
  - 先確認理解
  - 不主動產出完整文案
  - 適合用數字選項快速互動
- 只有在使用者明確要求開始產出 / 確認時，才進入完整寫作。

## 產出規則
- 進入產出階段後，內容需完整、不精簡。
- 預設先中文，確認後再翻譯。
- 文件應偏正式、可對外使用。

## 記憶持久化
- 使用 `discussion-notes.md` 保存進度。
- 當使用者說「我要離開」時：
  1. 更新 `discussion-notes.md`
  2. 記錄目前進度 / 已確認事項 / 待辦 / 下次方向
  3. 視情況 commit / push
  4. 回報簡短摘要
- 當使用者說「我回來了」時：
  - 讀取 `discussion-notes.md`
  - 回報上次進度、待辦、建議下一步

## 文件規範
- 檔名、資料夾名：英文、小寫、連字號。
- 多語結構：
  - `docs/zh/`
  - `docs/en/`
  - `docs/vi/`
- 先產出中文版，再做翻譯。

## 多語系與校正
- 日常修改以中文主檔為主。
- 若使用者要求「檢查校正」，至少檢查：
  - 語句
  - 拼字
  - 內容完整
  - 格式一致
  - 連結有效
  - 多語版本是否齊全
  - README 參照
  - 跨文件數據一致性

## 摘要圖模式
- 用於輸出投影片風格的摘要圖片。
- 輸出目錄：`~/jarvis/images/`
- 偏好：
  - 優先英文 prompt / 內容
  - 每張產 4 個變體
  - 先規劃內容再生成
- 此能力屬工具整合，API key 應放在本機環境，不進 repo。

## 投影片模式
- 使用 `2slides` 類流程生成正式簡報。
- 輸出目錄：`~/jarvis/slides/`
- 建議流程：
  1. 確認簡報主題與內容來源
  2. 搜模板
  3. 規劃大綱
  4. 確認後生成
  5. 提供本地檔案路徑
- 較複雜簡報優先 async 模式。

## MCP / 本機設定
- `image-generator` 與 `2slides` 屬本機 / 私有 MCP 設定。
- 不要把 API key、MCP URL token、私有路徑硬寫回 repo 文件。
- 需要時回讀：
  - `docs/mcp-config.example.json`
  - `docs/setup.md`
  - `mcp/` 目錄

## 協作原則
- 不猜測；資訊不足先確認。
- 討論時精簡，產出時完整。
- 重要進度及時保存。